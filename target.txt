                               <p className="text-[8px] text-zinc-400 font-bold uppercase">Countries</p>
                               {(materialDetails[selectedMaterial.name]?.countries || []).map((c, i) => (
                                 <div key={i} className="flex justify-between text-[8px] text-zinc-300">
                                   <span>{c.name}</span>
                                   <span>{c.amount}</span>
                                 </div>
                               ))}
